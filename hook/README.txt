Pour utiliser le hook pre-commit, il faut tout d'abord

	1.Copier celui ci dans .git/hook ou executer la commande suivante depuis la racine
	  cp hook/pre-commit .git/hook
	2.S'assurez des droit du fichier
	  chmod +x .git/hooks/pre-commit

